<view class="section-card">
  <view wx:if="{{showHeader}}" class="card-header" bindtap="onHeaderTap" hover-class="{{headerClickable ? 'card-header-active' : ''}}">
    <slot name="header"></slot>
  </view>
  <view class="card-body">
    <slot name="body"></slot>
  </view>
</view>
